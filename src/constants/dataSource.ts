import { DataSource } from '@/types/dataSource';

export const DEFAULT_DATA_SOURCE: DataSource = DataSource.Local;
export const STORAGE_KEY_DATA_SOURCE = 'flashcard_data_source';
export const SOURCE_QUERY_PARAM = 'source';
